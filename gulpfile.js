/* global require, process, console */
'use strict';

var gulp = require('gulp'),
    path = require('path'),
    prefs = require('./preferences.json'),
    $ = require('gulp-load-plugins')();


gulp.task('template', function(next) {
});

gulp.task('styles', function(next) {
});

gulp.task('server', function(next) {
});

gulp.task('default');
